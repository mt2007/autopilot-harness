# Autopilot Planning

Frontier format: `❓ Qn` + `➡️` recommended answer each round.

1. Discuss requirements (brownfield: read README/deps first).
2. When title is clear → create `plans/<slug>/` (brief.md, plan.md, checklist.md).
3. When frontier empties → checklist draft; user confirms → finalize `- [ ] <id> — <title>`.
4. Do **not** write product code in Planning; prompt `/autopilot-run`.
5. User may say "skip grill / use your recommendations" to shorten rounds; still produce the three artifacts.
